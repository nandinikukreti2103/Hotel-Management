server:
  port: 8082

spring:
  config:
    import: optional:configserver:http://localhost:8085

  application:
    name: HOTEL-SERVICE

  datasource:
    url: jdbc:mysql://localhost:3306/microservices_2
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true




